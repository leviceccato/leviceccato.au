{
  "private": true,
  "scripts": {
    "start": "vite",
    "check": "tsc --noEmit",
    "build": "bun run check && vite build",
    "preview": "bun run build && vite preview"
  },
  "devDependencies": {
    "lightningcss": "^1.22.0",
    "vite": "^4.5.0"
  }
}
